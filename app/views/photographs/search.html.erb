<%= form_with(url: search_photographs_path(@photographs), local: true, method: :get, class: "search-form") do |form| %>
  <%= form.text_field :keyword, placeholder: "検索する", class: "search-input" %>
  <%= form.submit "検索", class: "search-btn" %>
<% end %>
<div class="contents row">
  <% @photographs.each do |photograph| %>
    <%= render partial: "photograph", locals: { photo: photograph } %>
  <% end %>
</div>

<div class=back_btn>
    <%= link_to :back, class: "back_btn_link" do %>
      <%= image_tag('back_black_36dp.png', size: '185x50', class: "back-btn-icon") %>
      <div class= back_btn_text>back</div>
    <% end %>
</div>